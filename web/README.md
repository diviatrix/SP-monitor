# Web Assets

This directory contains the web interface assets for the Port Monitor Service:

- `index.html` - The main HTML template with Go template syntax
- `styles.css` - The CSS stylesheet for the dashboard